# PEID
Provides various functions that can attach to the provided tensor-train cross function, as well as tntorch.cross().

The python files folder includes all necessary files. In order to run the provided version of TT-Cross, you only need to load tt_functions.py. If you wish to use TnTorch, the files indexingfunctions.py and osfunctions.py need to be loaded.
